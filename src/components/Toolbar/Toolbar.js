import React from 'react';
import GithubLinkButton from './GithubLinkButton';
import Title from './Title';

const Toolbar = () => (
  <>
    <Title title="Bing map with markers and polygons" />
    <GithubLinkButton />
  </>
);

export default Toolbar;
